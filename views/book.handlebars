
<div class="books-container">
    
    <div>
        {{#with book}}
        <p>
            ID - {{id}}
        </p>
        <h3>
            {{title}}
        </h3>
        <p>Uma obra escrita em {{qtpages}} páginas</p>
        <a href="/edit/{{id}}">Editar</a>
        <form action="/deletebook/{{id}}" method="post">
            <input type="submit" value="Remover" class="deletebook">
        </form>
        {{/with}}
    </div>
</div>